# 次の関数定義には多くのバグがあります。
# スクリプトを実行し、Pythonのエラーヒントに従ってバグをすべて修正してください。
# 修正すると、この関数は名前を入力として受け取り、
# あいさつメッセージを返します。この戻り値は変数に保存できます。

function say_hello("name"):
        return print(f"Hello {name}!")

greeting = hello(name)
print(greeting)
